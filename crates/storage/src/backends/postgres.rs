//! Postgres backend (future). To implement: add a `PostgresStorage`, implement
//! [`crate::Storage`] for it, and wire it into [`crate::open`]. Config shape lives
//! at `[database.postgres]` in `apollo-config`.
