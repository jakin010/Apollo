//! SurrealDB backend (future). To implement: add a `SurrealdbStorage`, implement
//! [`crate::Storage`] for it, and wire it into [`crate::open`]. Config shape lives
//! at `[database.surrealdb]` in `apollo-config`.
